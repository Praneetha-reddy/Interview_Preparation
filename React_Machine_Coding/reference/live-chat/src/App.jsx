import ChatApp from "./ChatApp";

// App is the root component of the demo application.
// Its job is intentionally small so the chat logic remains focused inside ChatApp.
export default function App() {
  return <ChatApp />;
}
